/// array first program ///

#include<stdio.h>
#include<conio.h>

int main()
{
    int values[5] = {4,20,43,9,3};

    printf("\n this the 1 value = %d.",values[0]);
    printf("\n this the 2 value = %d.",values[1]);
    printf("\n this the 3 value = %d.",values[2]);
    printf("\n this the 4 value = %d.",values[3]);
    printf("\n this the 5 value = %d.",values[4]);

    getch();
    return 0;

}
